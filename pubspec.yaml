name: active_cf_rules
description: A new Flutter project.

# The following line prevents the package from being accidentally published to pub.dev using `flutter pub publish`.
publish_to: 'none'

# The version number can be set to any valid semver version.
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Add other dependencies here as needed

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Add other dev dependencies here as needed

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - assets/images/

  # To add custom fonts to your application, add a fonts section here,
  # along with a family name. Provide the asset paths for the font files:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
